Home exercise - Galor Siboni

CAN protocol messages exercise in python, My solution include the following:

Working code.

Code is well written (parameter names, function names, clear design, easy to maintain).

Implementation:

Tests and UT.

Error handling.

Logs.

Clear user functionality.

Edge case coverage.



# In order to run the unit test, The while loop needs to become a comment ( Lines 177-179 ) and the test.py should be the file you run.
# In order to run the code that Generate random traffic of CAN 2.0A messages
 that pass in the Detection unit and finish at the Reporting unit the while loop should be uncommented and the main.py should be the file you run.
